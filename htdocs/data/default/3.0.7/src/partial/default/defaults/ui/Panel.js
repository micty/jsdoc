/**
* Panel 模块的默认配置
* @name Panel.defaults
*/
define('Panel.defaults', /**@lends Panel.defaults*/ {

    /**
    * 是否在组件 render 后自动调用 show() 方法以进行显示。
    */
    showAfterRender: true,

    /**
    * 组件用到的 css 类名。
    */
    cssClass: '',

});

