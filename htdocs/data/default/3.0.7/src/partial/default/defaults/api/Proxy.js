/**
* Proxy 模块的默认配置
* @namespace
* @name defaults.Proxy
*/
define('defaults.Proxy', /**@lends defaults.Proxy*/ {
    
    base: '', //起始目录
    delay: {
        min: 500,
        max: 3000
    },
});

